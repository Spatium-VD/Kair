История коммитов и пояснения
============================

1) fde6ee9 - Initial project import.
   Что сделано:
   - Первый импорт проекта в репозиторий.
   - Добавлены исходники фронтенда и Google Apps Script.
   Зачем:
   - Зафиксировать стартовую рабочую версию проекта в Git.

2) 369fd3e - Update ignore rules for local artifacts and spreadsheet sources.
   Что сделано:
   - Обновлен .gitignore.
   - Добавлены/оставлены исключения для Excel-файлов и локальных служебных файлов
     (например, *.xlsx, *.xls, .DS_Store, .env, логи, временные файлы).
   Зачем:
   - Чтобы в GitHub не попадали исходные таблицы и локальный мусор.
   - Чтобы репозиторий оставался чистым и безопасным для публикации.

3) 0d46e76 - release: стабильная версия личного кабинета (ФОТ + премии)
   Что сделано:
   - Фронт: вход по ФИО + PIN из managers.csv, дашборд ФОТ + премии по проектам (с июня 2025).
   - Фильтр городов из CSV, спец-правила для «Самокат», кэш/перезагрузка списка при входе.
   - GAS: getBonusProjects, кэш CacheService, парсинг листов «Премия».
   - Брендинг Синержи, logo, фавикон, стили, README; тег stable на этом коммите.
   Зачем:
   - Зафиксировать рабочую «стабильную» сборку до переноса менеджеров в Google Таблицу.

4) b0f1b69 - feat: менеджеры с листа Google «managers», пресеты дат, config
   Что сделано:
   - GAS: action getManagers — чтение листа «managers» в той же книге, что ФОТ (без managers.csv на хостинге).
   - Фронт: загрузка менеджеров через API; сравнение PIN как строка; тексты ошибок и подсказок обновлены.
   - Дашборд: блок «Период» с быстрыми кнопками (месяц, квартал, год и т.д.), кнопки «Показать» / «Сбросить даты».
   - managers.example.csv, README-HOSTING.txt, правки README и .gitignore; обновлён config.js (URL Web App).
   Зачем:
   - Не хранить PIN в репозитории/zip для хостинга; править список только в Google Таблице.
   - Удобнее фильтровать выплаты по датам.

5) 292d003 - chore: секреты вне Git — config.example.js, ID таблиц в Script properties
   Что сделано:
   - В Git больше нет config.js: в репозитории config.example.js, рабочий config.js в .gitignore.
   - Code.gs читает PAYMENTS_SHEET_ID и BONUSES_SHEET_ID только из Script properties в Google.
   - README и README-HOSTING: как поднять проект и раздел про публичный GitHub + историю коммитов.
   Зачем:
   - Можно открыть репозиторий на GitHub без текущих URL и ID в последнем снимке (старая история всё ещё может их содержать).

6) ec565b2 - docs: COMMITS_EXPLAINED — коммит 292d003
   Что сделано: в этот файл добавлено описание пункта 5) выше.

7) 972b579 - chore: только нужные файлы в репозитории
   Что сделано:
   - Фавикон в репо как favicon.ico (ссылки в index/dashboard), длинное имя .ico убрано.
   - README-HOSTING.txt удалён — блок «Статический хостинг» перенесён в README.md.
   Зачем:
   - В GitHub остаётся минимум: фронт, Code.gs, примеры, README, COMMITS_EXPLAINED, logo.

8) 82aa43b - feat: GitHub Pages — config.js из секрета APPS_SCRIPT_URL
   Что сделано:
   - .github/workflows/pages.yml: сборка _site, config.js из secrets, деплой Pages.
   - README: раздел про GitHub Pages и секрет.
   Зачем:
   - На Pages не было config.js (он в .gitignore); без Actions URL некуда подставить.

9) e6e28f6 - revert: config.js в репо, GitHub Actions Pages убран
   Что сделано:
   - config.js с URL Web App снова в Git; .gitignore без исключения config.js.
   - Удалён .github/workflows/pages.yml — Pages: Deploy from branch main.
   - README упрощён (без секретов Actions).
   Зачем:
   - Проще: ссылка на Apps Script видна всем, как захотел владелец.

Примечание: новые коммиты дописывайте по номеру (10), (11)…

